import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const user = () => {
  return (
    <View>
      <Text style={{color: '#000000', marginTop: 20}}>user</Text>
    </View>
  );
};

export default user;

const styles = StyleSheet.create({});
